<%= render 'shared/admins' do %>
  <section class="section">
    <h2 class="title is-5 has-text-centered">
      Dodajesz wydarzenie
    </h2>
    <div class="container">
      <%= form_with model: @event, url: admin_events_path, method: :post, data: { turbo: false } do |f| %>
        <%= render 'admin/events/form', f: f %>
      <% end %>
    </div>
  </section>
<% end %>

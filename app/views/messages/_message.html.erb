<div id="message_<%= message.id %>" class="box mb-2">
  <strong><%= message.user.name %>:</strong>
  <%= simple_format(message.content) %>
  <small class="is-size-7 has-text-grey">
    <%= message.created_at.strftime("%H:%M:%S") %>
  </small>
</div>